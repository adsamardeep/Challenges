# 🏷 Increment an Integer Represented as an Array of Digits

## 📜 Problem Statement
You are given a non-empty list of digits representing a non-negative integer, where each digit is between 0 and 9. Increment the integer by one and return the resulting number as a list of digits.

## 🔍 Example Input/Output
**Input:**
```python
digits = [1, 2, 3]
````

**Output:**

```python
[1, 2, 4]
```

Another example:

```python
digits = [9, 9]
```

Output:

```python
[1, 0, 0]
```

## 🧠 Approach

1. Convert the list of digits into a string.
2. Convert the string into an integer.
3. Add `1` to the integer.
4. Convert the result back into a string.
5. Map each character of the string back to an integer and return it as a list.

This approach leverages Python’s built-in big integer handling for simplicity.

## 💻 Code 

```python
from typing import List

class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        return list(map(int, str(int(''.join(map(str, digits))) + 1)))
```

## ⏱ Complexity Analysis

* **Time Complexity:** `O(n)`
  Where `n` is the number of digits (conversion operations).
* **Space Complexity:** `O(n)`
  Due to creation of strings and the output list.

## 🧪 Edge Cases

* All digits are `9` (e.g., `[9, 9, 9]`)
* Single-digit input (e.g., `[0]`)
* Very large number of digits (handled safely by Python integers)

```
```
